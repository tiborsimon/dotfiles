" Add this ledger format before every other formats to be able to match it.
" There is already a similar format (%Y-%m-%d) that would match otherwise, if
" this format comes after it, and the weekday part won't be updated.
1SpeedDatingFormat %Y-%m-%d - %A    " Custom ledger file format
