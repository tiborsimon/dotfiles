if [ $(hostname) = 'tibor-ThinkPad-X240' ]; then
  xgamma -q -rgamma 1.1 -ggamma 0.9 -bgamma 0.9
fi

# typing repetion speed icrease
xset r rate 250 83 > /dev/null

# map caps lock to ctrl
setxkbmap -option ctrl:nocaps

if [ -f ~/.bashrc ]; then
   source ~/.bashrc
fi
