if [ $(hostname) = 'tibor-x240' ]; then
  xgamma -q -rgamma 1.5 -ggamma 1.2 -bgamma 1.1
fi

# typing repetion speed icrease
xset r rate 200 73 > /dev/null

# map caps lock to ctrl
setxkbmap -option ctrl:nocaps

if [ -f ~/.bashrc ]; then
   source ~/.bashrc
fi
