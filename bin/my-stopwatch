#! /bin/bash

KEZDES=$(date +%s) TZ=UTC0 watch -n 1 -t 'for i in $(seq $(($(tput lines) / 2 - 3)) ); do echo ""; done; figlet -c -f lean -w $(tput cols) $(printf "%(%H : %M : %S)T\n" $(($(date +%s) - $KEZDES)))'
