#!/usr/bin/env bash

WORKLOG_PATH="$HOME/projects/worklog/"
python ~/.dotfiles/bin/wl.py $WORKLOG_PATH
cd $WORKLOG_PATH
git commit -am "Worklog entry added."
git push
if [ $? -ne 0 ]; then
    echo "Something went wrong.."
else
    echo "Done"
fi
